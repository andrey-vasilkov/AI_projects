# Mini CRM — заявки

React + TypeScript + Vite. Данные хранятся в `localStorage`.

## Запуск

```bash
npm install
npm run dev
```

Открой `http://localhost:5173` в браузере.

## Сборка

```bash
npm run build
npm run preview
```
